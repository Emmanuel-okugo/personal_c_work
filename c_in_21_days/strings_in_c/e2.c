/* write a line of code that declares an array of type char, and initialize it to the string "pointers are fun". make the array just large enough to hold the string*/

#include <stdio.h>

char string[20] = " pointers are fun ";

int main(void)
{
printf("%s\n", string);

return(0);
}
