/*Write a line of code that allocatees storage for the string "pointers are fun!", as in exercise 2, but without using an array */

#include <stdio.h>

char *pointer = "Pointers are fun!";

int main(void)
{
	puts(pointer);

	return(0);
}

