/* Write code that performs two tasks: defines a structure named data that contains one type int member and two float members, and declares an instance of type data named info */

#include <stdio.h>

/**
 * main - entry point
 * description: above
 * Return: always 0 (success)
 */

struct data
{
	int one;
	float two;
	float three;
}info;

int main(void)
{
	return(0);
}
