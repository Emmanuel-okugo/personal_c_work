/* Create a structure containing five strings: address1, address2, city, state, and zip. create a typedef caled RECORD that can be used to create instances of this structure */

#include<stdio.h>

typedef struct
{
	*address1;
	*address2;
	*city;
	*state;
	*Zip;
}RECORD;

int main(void)
{
	return(0);
}
