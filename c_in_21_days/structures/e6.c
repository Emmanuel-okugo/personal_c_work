/* Write  the definition for a structure of type named data that can hold a single string of up to 20 characters */

#include <stdio.h>

/**
 * main - entry point
 * description: above
 * Return: alaways 0 (success)
 */

int main(void)
{
	struct data
	{
		char able[20];
	};

	return(0);
}
